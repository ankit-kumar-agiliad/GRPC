syntax = "proto3";

package cenbackend;

service CenBackend {
  rpc GotFromCip(stream CipMessage) returns (stream Censtatus);
}

message Censtatus {
  string message = 1;
}

message CipMessage {
  string message = 1;
}